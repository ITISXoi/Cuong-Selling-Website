export enum routeEnums {
  home = "/",
  receivers = "/receivers",
  jobs = "/jobs",
  receiver = "/receiver",
  location = "/location",
  history = "/history",
  watchlist = "/watch-list",
  vendors = "/vendors",
  detail = "product/:id",
  cart = "cart",
  allOrder = "old-order",
  oldOrder = "old-order/:id",
}
