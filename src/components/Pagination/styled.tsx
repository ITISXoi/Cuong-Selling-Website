import { styled } from "@mui/material";

const BoxWrap = styled("div")(() => ({
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    marginTop: 20
}));

export { BoxWrap };
